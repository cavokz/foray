journal  = "stats-single"
archived = false
prompt   = "Read the stats-single journal and describe the finding."

[[expected_behaviors]]
description = "Calls list_journals before sync_journal."

[[expected_behaviors]]
description = "avg_item_size is present in the list_journals response; std_item_size is Some(0) — the formula floor(budget / (avg + 2 * 0)) reduces to floor(budget / avg)."

[[expected_behaviors]]
description = "Uses the formula with std_item_size = 0 to compute page size — does not fall back to size: 5 or use a hardcoded round number."

[[expected_behaviors]]
description = "Makes one sync_journal call and retrieves the single item."

[[expected_behaviors]]
description = "Describes the finding: auth session cookie not invalidated on logout."
