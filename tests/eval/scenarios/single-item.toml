journal  = "stats-single"
archived = false
prompt   = "Read the stats-single journal and describe the finding."

[[expected_behaviors]]
description = "Calls list_journals before sync_journal."

[[expected_behaviors]]
description = "avg_item_size is present in the list_journals response; std_item_size is absent (only 1 item)."

[[expected_behaviors]]
description = "Handles absent std_item_size by using size: 5 as the safe default — does not use an avg-only formula or a large round number."

[[expected_behaviors]]
description = "Makes one sync_journal call and retrieves the single item."

[[expected_behaviors]]
description = "Describes the finding: auth session cookie not invalidated on logout."
